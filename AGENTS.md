# Klar repository instructions

For all future Klar build guides, treat this repository (`/Users/kumararpit/klar`) as the authoritative current implementation state.

- Inspect the repository files directly before designing the next release.
- Verify the current baseline by running the relevant tests, type checks, and production build.
- Use the roadmap for release requirements.
- Use previous build guides only for historical context and migration constraints.
- Do not reconstruct the current application from previous build guides.


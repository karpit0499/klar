# Generated reports

Do not commit generated benchmark reports here. Store them with the controlled
release evidence for the exact Klar source commit, model checksum, runtime
checksum and adapter checksums that produced them.

The source tree contains only the fixed synthetic fixtures, harness and report
schema. A missing run must remain `hold` or `not_run`; this directory is not
evidence that a benchmark happened.
